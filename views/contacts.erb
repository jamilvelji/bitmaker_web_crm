<h1>All Contacts</h1>

<ul>
	<% @contacts.each do |contact| %>
	<li><%= contact.first_name %> <%= contact.last_name %> (<a href=mailto:<%= contact.email %>><%= contact.email %></a>)</li>
	<% end %>
</ul>